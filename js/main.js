// App entry point - initializes app

/*
This imports the entire module (listeners.js) and executes it.
Any top-level code (like addEventListener("DOMContentLoaded", ...)) 
in listeners.js runs immediately upon import.
*/
// import * as listeners  from "./listeners.js";
import "./listeners.js";

